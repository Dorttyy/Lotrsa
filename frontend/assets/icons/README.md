# Uploaded send icon

`send.png` is the user's original transparent Icons8 PNG, unchanged, 512×512,
7099 bytes. SHA256: `28f7b1953f4ecc712c3acb86301ee5f375811393bdac5531b42000ce8d76784a`.

Canonical managed object-storage upload (provided by the user):
https://customer-assets-v7afamib.emergentagent.net/job_elevate-familiar/artifacts/uei5iyvb_icons8-send-512.png

The bundled copy allows the icon to work offline. `SendGlyph` crops only the
transparent outer margins in its viewport and applies the existing button's
tint. `send`, `send-outline`, `paper-plane` and `paper-plane-outline` aliases
across both icon families resolve to this same uploaded artwork. No message
handlers, disabled/loading states or button hit targets are changed.